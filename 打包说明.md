# Porhead 作品集打包说明

## 为什么直接发文件包看不到你的修改

编辑模式目前会把修改保存在浏览器的本地缓存里。  
如果只是把旧的 `dist` 或旧 zip 发给朋友，朋友电脑上没有你的浏览器缓存，所以会看到修改前的内容。

现在项目已经加了自动同步：

1. 用 `START-PORHEAD.bat` 启动网站。
2. 打开页面，你的修改会自动写入项目里的 `src/saved-edits.json`。
3. 也可以在编辑模式里点 `同步修改到项目` 手动保存。

## 发给朋友

双击 `PACKAGE-FRIEND.bat`。  
它会用你当前保存的修改重新构建，并生成：

- `Porhead-For-Friend.zip`

把这个单独发给朋友即可。朋友解压后双击 `START-PORHEAD.bat` 就能打开。

## 上传 GitHub

直接提交整个项目源码到 GitHub 仓库即可。  
注意不要提交：

- `node_modules`
- `dist`
- `runtime`
- `Porhead-*.zip`

项目里已经放好 `.gitignore`。

如果使用 GitHub Pages，项目里已经包含：

- `.github/workflows/deploy.yml`

你只需要在 GitHub 仓库里开启：

- Settings → Pages → Source 选择 `GitHub Actions`

之后每次 push 到 `main`，GitHub 会自动构建并部署。

## 手动构建静态文件

双击 `BUILD-STATIC.bat`。  
构建完成后，把 `dist` 文件夹里的内容上传到：

- GitHub Pages
- Netlify
- Vercel
- 任意静态网站托管服务
